def find_it(seq):
    for i in seq:
        count = 0
        for j in range(len(seq)):
            if seq[j] == i:
                count += 1
        if (count % 2 != 0):
            return i
            break
